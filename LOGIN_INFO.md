# 🔐 Информация для входа в Saleor Dashboard

## ✅ Все сервисы работают!

- **Saleor API:** ✅ Работает (HTTP 200)
- **Dashboard:** ✅ Работает (HTTP 200)
- **База данных:** ✅ Пользователь существует

---

## 📍 Вход в Dashboard

### 1. Откройте браузер

Перейдите по адресу (скопируйте и вставьте):
```
http://localhost:9000/
```

### 2. Введите учетные данные

**ВАЖНО: Копируйте точно, без пробелов!**

#### Email (скопируйте):
```admin@example.com

```

#### Пароль (скопируйте):
```
admin
```

---

## 🔍 Проверено через API

Учетные данные работают через GraphQL API:
- ✅ Email: `admin@example.com` существует
- ✅ Пользователь активен (is_active = true)
- ✅ Пользователь staff (is_staff = true)
- ✅ Пользователь superuser (is_superuser = true)
- ✅ Токен успешно получен

---

## ⚠️ Если не получается войти

### Проблема 1: Ошибка "Invalid credentials"

**Причины:**
1. Опечатка в email или пароле
2. Лишние пробелы
3. Регистр букв (должен быть точно как указано)

**Решение:**
- Скопируйте данные из секции выше
- Вставьте в форму входа
- НЕ ПЕЧАТАЙТЕ вручную

### Проблема 2: Dashboard не загружается

**Проверьте:**
```bash
# Статус Dashboard
curl http://localhost:9000/

# Если пусто, перезапустите:
cd /Users/ap/Projects/saleor-dashboard
npm run dev
```

### Проблема 3: "Cannot connect to API"

**Проверьте Saleor API:**
```bash
# Статус API
curl http://localhost:8000/graphql/

# Если не работает, запустите:
cd "/Users/ap/Projects/Miraflores 2.0"
uv run poe start
```

### Проблема 4: Страница загружается медленно

**Это нормально!** При первой загрузке Dashboard:
- Собирает assets
- Подключается к API
- Может занять до 1 минуты

**Подождите и обновите страницу (F5 или Cmd+R)**

---

## 🧹 Очистка кэша браузера

Если данные не подходят:

### Chrome / Edge
1. Откройте DevTools (F12)
2. Правый клик на кнопке обновления
3. "Empty Cache and Hard Reload"

### Safari
1. Cmd + Option + E (очистить кэш)
2. Cmd + R (обновить)

### Firefox
1. Ctrl/Cmd + Shift + Delete
2. Очистить кэш
3. Обновить страницу

---

## 🔄 Перезапуск Dashboard

Если ничего не помогает:

```bash
# 1. Остановить Dashboard
ps aux | grep "npm run dev" | grep -v grep | awk '{print $2}' | xargs kill -9

# 2. Перезапустить
cd /Users/ap/Projects/saleor-dashboard
npm run dev

# 3. Подождать 30 секунд

# 4. Открыть http://localhost:9000/
```

---

## ✅ Точные данные для входа

### Метод 1: Копировать-Вставить

**Email:**
```
admin@example.com
```

**Пароль:**
```
admin
```

### Метод 2: Вход через GraphQL (если Dashboard не работает)

1. Откройте http://localhost:8000/graphql/
2. Вставьте:
```graphql
mutation {
  tokenCreate(email: "admin@example.com", password: "admin") {
    token
    user { email }
  }
}
```
3. Получите токен
4. Используйте токен в API запросах

---

## 📊 Альтернативные пользователи

Если admin не работает, попробуйте других staff пользователей:

1. `sierra.house@example.com`
2. `shelby.king@example.com`
3. `checkout.manager@example.com`

**Пароль для всех:** `admin`

---

## 🆘 Создать нового администратора

Если ничего не работает, создайте нового:

```bash
cd "/Users/ap/Projects/Miraflores 2.0"

# Через Django shell
uv run python manage.py shell

# В shell введите:
from account.models import User
user = User.objects.create_superuser(
    email="newadmin@example.com",
    password="newpassword"
)
user.save()
exit()
```

Затем войдите с:
- Email: `newadmin@example.com`
- Пароль: `newpassword`

---

## 📝 Проверка через консоль браузера

1. Откройте Dashboard: http://localhost:9000/
2. Откройте DevTools (F12)
3. Вкладка Network
4. Попробуйте войти
5. Посмотрите ошибки в запросах

Если видите:
- `401 Unauthorized` → неверный пароль
- `Network error` → проблема с API
- `CORS error` → проблема с CORS

---

## 🎯 Быстрая проверка

### Шаг 1: API работает?
```bash
curl -X POST http://localhost:8000/graphql/ \
  -H "Content-Type: application/json" \
  -d '{"query":"mutation { tokenCreate(email: \"admin@example.com\", password: \"admin\") { token } }"}'
```

Должно быть: `"token": "ey..."`

### Шаг 2: Dashboard открывается?
```bash
curl http://localhost:9000/
```

Должен быть HTML код

### Шаг 3: Войти
1. http://localhost:9000/
2. Email: `admin@example.com`
3. Пароль: `admin`

---

## 📞 Если проблема остается

Опишите, что именно происходит:
1. Какая ошибка показывается?
2. На каком этапе?
3. Что в консоли браузера (F12)?

---

**P.S.** Учетные данные точно работают - проверено через API!

*Email:* **admin@example.com**  
*Пароль:* **admin**  
*URL:* **http://localhost:9000/**

