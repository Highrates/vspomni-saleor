# ✅ MCP для Cursor установлен и настроен!

## 🎉 Model Context Protocol успешно настроен

MCP позволит AI в Cursor напрямую работать с базой данных и файловой системой проекта Saleor.

---

## 📋 Что было сделано

### ✅ Создана конфигурация MCP
Файл: `~/.cursor/mcp.json`

**Подключенные серверы:**

1. **saleor-db** - доступ к PostgreSQL
   - База: 142 таблицы Saleor
   - Connection: `postgresql://saleor:saleor@localhost:5432/saleor`

2. **saleor-project** - доступ к файлам проекта
   - Путь: `/Users/ap/Projects/Miraflores 2.0`
   - Включает весь код проекта

---

## 🚀 Как использовать

### 1. Перезапустите Cursor
**Важно!** Чтобы изменения вступили в силу, полностью закройте и перезапустите Cursor.

```bash
# Закройте Cursor
# Затем откройте снова
```

### 2. Проверьте подключение MCP

После перезапуска Cursor, в AI чате спросите:

#### Примеры запросов к базе данных:
```
1. Покажи схему таблицы product_product
2. Сколько заказов в базе данных?
3. Покажи все таблицы в базе данных Saleor
4. Какие поля есть у таблицы order_order?
5. Найди пользователей с email admin@example.com
```

#### Примеры запросов к файловой системе:
```
1. Покажи все модели в saleor/product/models.py
2. Найди все GraphQL мутации для работы с заказами
3. Какие тесты есть для checkout модуля?
4. Покажи структуру директории saleor/graphql/
```

---

## 🔍 Как проверить, что MCP работает

### Признаки успешного подключения:

1. **В Cursor появятся новые инструменты** для AI
2. **AI сможет читать БД напрямую** без вашей помощи
3. **AI будет знать содержимое файлов** автоматически

### Тестовый запрос:
```
Покажи мне схему таблицы product_product из базы данных
и найди модель Product в файле saleor/product/models.py
```

Если MCP работает, AI:
- Прочитает схему таблицы из PostgreSQL
- Найдет и покажет модель из файла
- Сделает это **автоматически**, без запроса файлов

---

## 📊 Конфигурация MCP

### Просмотр конфига:
```bash
cat ~/.cursor/mcp.json
```

### Содержимое:
```json
{
  "mcpServers": {
    "saleor-db": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-postgres",
        "postgresql://saleor:saleor@localhost:5432/saleor"
      ]
    },
    "saleor-project": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-filesystem",
        "/Users/ap/Projects/Miraflores 2.0"
      ]
    }
  }
}
```

---

## 🔧 Доступные MCP серверы

### 1. PostgreSQL (saleor-db)
- **Что дает:** Прямой доступ к схеме и данным БД
- **Возможности:**
  - Чтение схемы таблиц
  - Выполнение SELECT запросов
  - Анализ данных

### 2. Filesystem (saleor-project)
- **Что дает:** Доступ ко всем файлам проекта
- **Возможности:**
  - Чтение содержимого файлов
  - Поиск по файлам
  - Анализ структуры проекта

---

## 💡 Примеры использования MCP

### Анализ базы данных
```
AI запрос: "Покажи мне структуру таблицы product_product"

AI ответ: 
- Прочитает схему из PostgreSQL
- Покажет все колонки с типами
- Объяснит связи с другими таблицами
```

### Работа с кодом
```
AI запрос: "Найди все GraphQL мутации для создания продукта"

AI ответ:
- Найдет файлы в saleor/graphql/product/mutations/
- Покажет код мутаций
- Объяснит, как они работают
```

### Комплексный анализ
```
AI запрос: "Покажи связь между моделью Product и таблицей product_product"

AI ответ:
- Прочитает модель из models.py
- Прочитает схему из БД
- Покажет соответствие полей
```

---

## 🛡️ Безопасность

### ⚠️ Важно:
- MCP конфиг содержит credentials к БД
- Файл `~/.cursor/mcp.json` только для локальной разработки
- **НЕ коммитьте** этот файл в Git
- Для production используйте read-only пользователя

### Создание read-only пользователя:
```bash
# Подключиться к PostgreSQL
docker-compose exec db psql -U saleor -d saleor

# Создать read-only пользователя
CREATE USER mcp_readonly WITH PASSWORD 'secure_password';
GRANT CONNECT ON DATABASE saleor TO mcp_readonly;
GRANT USAGE ON SCHEMA public TO mcp_readonly;
GRANT SELECT ON ALL TABLES IN SCHEMA public TO mcp_readonly;
ALTER DEFAULT PRIVILEGES IN SCHEMA public GRANT SELECT ON TABLES TO mcp_readonly;
```

Затем обновите конфиг:
```json
"postgresql://mcp_readonly:secure_password@localhost:5432/saleor"
```

---

## 🔍 Отладка

### MCP не работает?

1. **Проверьте, что Cursor перезапущен**
```bash
# Полностью закройте Cursor
# Откройте снова
```

2. **Проверьте доступность БД**
```bash
docker-compose exec db psql -U saleor -c "SELECT 1"
```

3. **Проверьте конфиг**
```bash
cat ~/.cursor/mcp.json
# Убедитесь, что JSON валидный
```

4. **Посмотрите логи Cursor**
```bash
# macOS
tail -f ~/Library/Application\ Support/Cursor/logs/*.log
```

5. **Протестируйте MCP сервер вручную**
```bash
npx -y @modelcontextprotocol/server-postgres \
  "postgresql://saleor:saleor@localhost:5432/saleor"
```

---

## 📈 Дополнительные возможности

### Добавить Redis (опционально)
```bash
# Отредактировать ~/.cursor/mcp.json
nano ~/.cursor/mcp.json
```

Добавить в `mcpServers`:
```json
"saleor-redis": {
  "command": "npx",
  "args": [
    "-y",
    "@modelcontextprotocol/server-redis",
    "redis://localhost:6379"
  ]
}
```

### Ограничить доступ к файлам
Вместо всего проекта можно дать доступ только к конкретной папке:
```json
"saleor-graphql": {
  "command": "npx",
  "args": [
    "-y",
    "@modelcontextprotocol/server-filesystem",
    "/Users/ap/Projects/Miraflores 2.0/saleor/graphql"
  ]
}
```

---

## 🎯 Что дальше?

### 1. Перезапустите Cursor (обязательно!)
```bash
# Закройте Cursor полностью
# Откройте снова
```

### 2. Протестируйте MCP
В AI чате напишите:
```
Покажи схему таблицы product_product из базы данных
```

### 3. Начните использовать
- Задавайте вопросы о структуре БД
- Просите AI найти код в проекте
- Анализируйте данные через AI

---

## 📚 Ресурсы

- **MCP документация:** https://modelcontextprotocol.io
- **Cursor MCP Guide:** https://cursor.com/docs/context/mcp
- **MCP серверы:** https://github.com/modelcontextprotocol/servers

---

## ✨ MCP установлен и готов к использованию!

**Следующий шаг:** Перезапустите Cursor и проверьте работу MCP с помощью тестового запроса.

### Тестовый запрос:
```
Покажи мне:
1. Схему таблицы product_product из БД
2. Модель Product из файла saleor/product/models.py
3. Сколько всего таблиц в базе данных?
```

Если AI ответит на все 3 вопроса без вашей помощи - **MCP работает!** 🎉

---

*Установлено: $(date)*
*База данных: 142 таблицы*
*Проект: /Users/ap/Projects/Miraflores 2.0*

