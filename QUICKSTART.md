# 🚀 Saleor Quick Start Guide

## ✅ Установка завершена!

Saleor успешно настроен и запущен на вашем локальном компьютере.

## 📍 Доступные URL

| Сервис | URL | Описание |
|--------|-----|----------|
| **GraphQL API** | http://localhost:8000/graphql/ | GraphQL Playground для тестирования API |
| **Mailpit** | http://localhost:8025/ | Веб-интерфейс для просмотра отправленных email |
| **PostgreSQL** | localhost:5432 | База данных |
| **Redis** | localhost:6379 | Кэш и брокер сообщений |

## 🔑 Учетные данные

### Администратор
- **Email:** `admin@example.com`
- **Пароль:** `admin`

### Тестовые пользователи
В базе данных создано 20 тестовых пользователей. Примеры:
- `melissa.sanders@example.com`
- `edward.cook@example.com`
- и другие...

## 🛠 Основные команды

### Управление сервером
```bash
# Запустить сервер Saleor
uv run poe start

# Запустить Celery worker (для фоновых задач)
uv run poe worker

# Запустить Celery Beat (планировщик задач)
uv run poe scheduler
```

### База данных
```bash
# Применить миграции
uv run poe migrate

# Создать новые миграции
uv run poe make-migrations

# Загрузить тестовые данные
uv run poe populatedb

# Django shell
uv run poe shell
```

### Тестирование
```bash
# Запустить все тесты
uv run poe test

# Запустить конкретный тест
uv run poe test saleor/graphql/product/tests/

# Один тест с отладкой
uv run poe test path/to/test.py::test_name -n0 -s
```

### Docker сервисы
```bash
# Запустить все сервисы
docker-compose up -d

# Остановить сервисы
docker-compose down

# Посмотреть логи
docker-compose logs -f db redis mailpit

# Перезапустить конкретный сервис
docker-compose restart db
```

## 📦 Установленные компоненты

- ✅ Python 3.12.12 (через uv)
- ✅ PostgreSQL 16 (в Docker)
- ✅ Redis 7 (в Docker)
- ✅ Mailpit (SMTP сервер для разработки)
- ✅ 235 Python пакетов
- ✅ Тестовые данные:
  - Продукты с вариантами
  - Категории и коллекции
  - Заказы
  - Пользователи и группы прав
  - Промоакции и ваучеры
  - Подарочные карты
  - Меню навигации

## 🔧 Настройка проекта

### Файлы конфигурации
- `.env` - переменные окружения
- `docker-compose.yml` - Docker сервисы
- `.cursorrules` - правила для AI ассистента Cursor
- `pyproject.toml` - зависимости и настройки проекта

### Важные переменные окружения (.env)
```bash
DEBUG=True
DATABASE_URL=postgres://saleor:saleor@localhost:5432/saleor
CELERY_BROKER_URL=redis://localhost:6379/0
EMAIL_URL=smtp://localhost:1025
DASHBOARD_URL=http://localhost:9000/
```

## 🎯 Первые шаги

### 1. Протестировать GraphQL API
1. Откройте http://localhost:8000/graphql/
2. Выполните тестовый запрос:
```graphql
{
  products(first: 5, channel: "default-channel") {
    edges {
      node {
        id
        name
        description
        thumbnail {
          url
        }
      }
    }
  }
}
```

### 2. Посмотреть отправленные email
1. Откройте http://localhost:8025/
2. Здесь будут отображаться все email, отправленные Saleor

### 3. Изучить структуру проекта
```bash
saleor/
├── account/      # Пользователи и аутентификация
├── product/      # Товары и каталог
├── order/        # Заказы
├── checkout/     # Корзина и оформление
├── payment/      # Платежи
├── graphql/      # GraphQL API
├── plugins/      # Система расширений
└── webhook/      # Вебхуки
```

## 📚 Дополнительные ресурсы

- **Документация Saleor:** https://docs.saleor.io
- **GraphQL Schema:** `/Users/ap/Projects/Miraflores 2.0/saleor/graphql/schema.graphql`
- **Discord сообщество:** https://saleor.io/discord
- **Contributing Guide:** `CONTRIBUTING.md`

## 🐛 Решение проблем

### Сервер не запускается
```bash
# Проверить, что все сервисы запущены
docker-compose ps

# Посмотреть логи
docker-compose logs -f
```

### База данных недоступна
```bash
# Перезапустить PostgreSQL
docker-compose restart db

# Проверить подключение
docker-compose exec db psql -U saleor -d saleor -c "SELECT 1"
```

### Ошибки миграций
```bash
# Откатить последнюю миграцию
uv run python manage.py migrate <app_name> <migration_name>

# Пересоздать БД (ВНИМАНИЕ: удалит все данные!)
docker-compose down -v
docker-compose up -d db
uv run poe migrate
uv run poe populatedb
```

## 💡 AI Контекст в Cursor

Файл `.cursorrules` содержит:
- Структуру проекта Saleor
- Стандарты кодирования
- Паттерны разработки
- Частые команды
- Примеры реализации

Cursor AI будет учитывать эти правила при генерации кода и подсказках.

## 🚦 Статус сервисов

Проверить статус:
```bash
# Docker сервисы
docker-compose ps

# Saleor API
curl http://localhost:8000/graphql/

# Mailpit
curl http://localhost:8025/
```

---

## 📝 Следующие шаги

1. **Изучите GraphQL API** - откройте Playground и попробуйте запросы
2. **Посмотрите тестовые данные** - в БД уже есть продукты, заказы, пользователи
3. **Запустите тесты** - `uv run poe test saleor/graphql/product/tests/`
4. **Создайте свою первую мутацию** - следуя паттернам в `.cursorrules`
5. **Настройте Dashboard** - https://github.com/saleor/saleor-dashboard

---

*Создано автоматически при первом запуске Saleor 3.23.0*

