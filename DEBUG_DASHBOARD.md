# 🔍 Диагностика Dashboard

## ✅ Что работает:

1. **Saleor API:** ✅ HTTP 200
2. **Dashboard:** ✅ HTTP 200  
3. **CORS:** ✅ Настроен правильно
4. **Аутентификация:** ✅ Токен создается
5. **База данных:** ✅ Пользователь существует

---

## 🚨 Возможные проблемы:

### 1. **404 ошибка в браузере**

**Что делать:**
1. Откройте **http://localhost:9000/**
2. Нажмите **F12** (DevTools)
3. Перейдите на вкладку **"Network"**
4. Обновите страницу (**Ctrl+R**)
5. Найдите запросы с **красным статусом** (404, 500, etc.)

**Что искать:**
- Запросы к `http://localhost:8000/graphql/` с ошибками
- Запросы к статическим файлам (CSS, JS) с 404
- CORS ошибки

### 2. **Dashboard не может подключиться к API**

**Проверьте в консоли браузера (F12 → Console):**
- Ошибки JavaScript
- CORS ошибки
- Network ошибки

### 3. **Проблема с аутентификацией**

**Проверьте:**
- Правильно ли введены email и пароль
- Нет ли лишних пробелов
- Регистр букв

---

## 🛠️ Пошаговая диагностика:

### Шаг 1: Проверьте браузер
```bash
# Откройте в браузере
http://localhost:9000/

# Откройте DevTools (F12)
# Вкладка Console - есть ли ошибки?
# Вкладка Network - есть ли красные запросы?
```

### Шаг 2: Проверьте API подключение
```bash
# В консоли браузера выполните:
fetch('http://localhost:8000/graphql/', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({ query: '{ shop { name } }' })
}).then(r => r.json()).then(console.log)
```

### Шаг 3: Проверьте аутентификацию
```bash
# В консоли браузера выполните:
fetch('http://localhost:8000/graphql/', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({ 
    query: 'mutation { tokenCreate(email: "admin@example.com", password: "admin") { token } }' 
  })
}).then(r => r.json()).then(console.log)
```

---

## 🔧 Быстрые исправления:

### 1. Очистите кэш браузера
- **Chrome:** Ctrl+Shift+Delete → Очистить кэш
- **Safari:** Cmd+Option+E
- **Firefox:** Ctrl+Shift+Delete

### 2. Попробуйте режим инкогнито
- **Chrome:** Ctrl+Shift+N
- **Safari:** Cmd+Shift+N
- **Firefox:** Ctrl+Shift+P

### 3. Попробуйте другой браузер
- Chrome
- Firefox
- Safari

### 4. Перезапустите Dashboard
```bash
# Остановить
pkill -f "npm run dev"

# Запустить заново
cd /Users/ap/Projects/saleor-dashboard
npm run dev
```

---

## 📋 Что проверить в DevTools:

### Console (F12 → Console)
- **Ошибки JavaScript** (красный текст)
- **CORS ошибки** (Access-Control-Allow-Origin)
- **Network ошибки** (Failed to fetch)

### Network (F12 → Network)
- **Запросы к API** (http://localhost:8000/graphql/)
- **Статус ответов** (200, 404, 500)
- **Время загрузки** (медленные запросы)

### Application (F12 → Application)
- **Local Storage** (сохраненные токены)
- **Session Storage** (временные данные)
- **Cookies** (аутентификация)

---

## 🆘 Альтернативные решения:

### 1. Создать нового пользователя
```bash
cd "/Users/ap/Projects/Miraflores 2.0"
uv run python manage.py shell

# В shell:
from account.models import User
user = User.objects.create_superuser(
    email="test@example.com",
    password="test123"
)
user.save()
exit()
```

**Войти с:**
- Email: `test@example.com`
- Пароль: `test123`

### 2. Использовать GraphQL Playground
```bash
# Откройте в браузере
http://localhost:8000/graphql/

# Выполните запрос:
mutation {
  tokenCreate(email: "admin@example.com", password: "admin") {
    token
    user { email }
  }
}
```

### 3. Проверить через curl
```bash
# Тест API
curl -X POST http://localhost:8000/graphql/ \
  -H "Content-Type: application/json" \
  -d '{"query":"query { shop { name } }"}'

# Тест аутентификации
curl -X POST http://localhost:8000/graphql/ \
  -H "Content-Type: application/json" \
  -d '{"query":"mutation { tokenCreate(email: \"admin@example.com\", password: \"admin\") { token } }"}'
```

---

## 📞 Если ничего не помогает:

**Опишите:**
1. **Какая ошибка** показывается в браузере?
2. **Что в консоли** (F12 → Console)?
3. **Что в Network** (F12 → Network)?
4. **На каком этапе** происходит ошибка?

**Скриншоты:**
- Страница входа
- Ошибка в консоли
- Network вкладка

---

## 🎯 Точные данные для входа:

**URL:** http://localhost:9000/  
**Email:** admin@example.com  
**Пароль:** admin

**Если не работает - проверьте DevTools! 🔍**
